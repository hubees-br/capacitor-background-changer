{
  "name": "capacitor-background-changer",
  "version": "10.0.0",
  "description": "Capacitor plugin to change WebView background to transparent",
  "main": "dist/plugin.js",
  "module": "dist/esm/index.js",
  "types": "dist/esm/index.d.ts",
  "scripts": {
    "build": "npm run clean && tsc && rollup -c rollup.config.js",
    "clean": "rimraf ./dist",
    "watch": "tsc --watch",
    "prepublishOnly": "npm run build"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@capacitor/core": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/android": "^6.1.2",
    "rimraf": "^3.0.2",
    "rollup": "^2.32.0",
    "typescript": "^4.0.3"
  },

  "files": [
    "dist/",
    "android/",
    "CapacitorBackgroundChanger.podspec"
  ],
  "keywords": [
    "capacitor",
    "plugin",
    "native"
  ],
  "capacitor": {
    "android": {
      "src": "android"
    }
  }
}